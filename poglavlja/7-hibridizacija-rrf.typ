= Хибридизација резултата претраге (_RRF_)

У претходним поглављима су описана два приступа претрази истог корпуса:
лексичка претрага заснована на алгоритму _BM25_ (поглавље 6) и
семантичка претрага заснована на _embedding_ векторима (поглавље 7).
Лексичка претрага даје предност поклапању термина из упита и документа,
док семантичка претрага омогућава проналажење садржаја сличног значења
и када се коришћене ријечи не поклапају. Због различитих начина
представљања текста и одређивања релевантности, ова два приступа могу
да се међусобно допуњују @luan2021sparse.

Luan и сарадници анализирају разлике између _sparse_ и _dense_
приступа претраживању и показују да они имају различите предности.
_Sparse_ репрезентације задржавају директну везу са терминима у тексту,
док _dense_ репрезентације омогућавају препознавање семантичке
сличности и када не постоји директно поклапање термина. Њихови
резултати показују и да комбиновање ова два приступа може да побољша
квалитет претраге @luan2021sparse. Обједињавање резултата лексичке и
семантичке претраге у јединствену ранг-листу представља један од
приступа хибридној претрази @elastichybrid.

У систему описаном у овом раду, резултати лексичке и семантичке
претраге се обједињују методом _Reciprocal Rank Fusion_ (_RRF_). У
наставку поглавља је описан принцип рада, те разлози избора ове методе,
а затим и начин њене примјене у имплементираном систему.


== _Reciprocal Rank Fusion_

_Reciprocal Rank Fusion_ (_RRF_) представља методу за спајање више
рангираних листа у јединствену ранг-листу @cormack2009. За разлику од
приступа који директно комбинују скорове различитих система, _RRF_ се
заснива на позицији резултата у свакој листи. Због тога није потребно
претходно усклађивати скале _BM25_ скора и косинусне сличности
@cormack2009.

За документ $d$ и скуп рангираних листа $R$, _RRF_ скор се дефинише као
@cormack2009:

$ "RRF"(d) = sum_(r in R) 1 / (k + "rank"_r(d)) $

гдје $"rank"_r(d)$ представља позицију документа $d$ у ранг-листи $r$,
док је $k$ константа којом се ублажава утицај разлика између највиших
позиција. Ако се документ не појављује у одређеној листи, та листа не
доприноси његовом _RRF_ скору.


=== Улога константе $k$

Константа $k$ ублажава разлику између доприноса резултата који се
налазе на највишим позицијама ранг-листе. Без ове константе би прва
позиција доприносила вриједношћу $1$, а друга $1/2$. Са $k = 60$,
њихови доприноси износе $1/61$ и $1/62$, па разлика између сусједних
позиција на врху листе има мањи утицај на коначни скор.

Cormack и сарадници су у оригиналном раду за _RRF_ користили
вриједност $k = 60$. Она је изабрана на основу пилот-експеримента, а
аутори наводе да се показала близу оптималне и да резултати нису били
нарочито осјетљиви на избор ове константе @cormack2009. Иста вриједност
је коришћена и у систему описаном у овом раду.

=== Интуиција кроз примјер

Претпоставимо да лексичка претрага за исти упит рангира епизоде $A$,
$B$ и $C$ на позиције 1, 2 и 3, док их семантичка претрага поставља на
позиције 2, 4 и 1. За $k = 60$ се добија:

$ "RRF"(A) = 1/(60+1) + 1/(60+2) = 0.03252 $

$ "RRF"(B) = 1/(60+2) + 1/(60+4) = 0.03175 $

$ "RRF"(C) = 1/(60+3) + 1/(60+1) = 0.03227 $

Коначан поредак је, према томе, $A > C > B$. Епизоде $A$ и $C$ имају
највећи скор јер су високо позициониране у обје листе. _RRF_ на тај
начин омогућава да резултат који је добро рангиран у више система
добије допринос од сваког од њих, без директног поређења њихових
оригиналних скорова.


== Избор _RRF_-а у односу на алтернативне приступе

При избору начина спајања резултата су разматрана два алтернативна
приступа: комбиновање оригиналних скорова лексичке и семантичке
претраге и додатно рангирање резултата помоћу _cross-encoder_ модела.


=== Комбиновање скорова

Један од начина спајања резултата јесте комбиновање _BM25_ скора и
косинусне сличности, на примјер њиховом пондерисаном сумом. Међутим,
ове вриједности нису директно упоредиве јер потичу из различитих модела
рангирања и могу имати различите расподјеле и опсеге. Због тога је при
оваквом приступу потребно претходно нормализовати скорове, на примјер
_min-max_ нормализацијом, а затим одредити тежину сваког сигнала
@opensearchhybrid.

_RRF_ избјегава потребу за усклађивањем скорова јер користи позицију
резултата у појединачним ранг-листама, а не њихове оригиналне
вриједности. Због једноставности и одсуства потребе за подешавањем
тежина, овај приступ је погодан за систем развијен у оквиру овог рада.


=== _Cross-encoder reranking_

Друга разматрана могућност је била примјена _cross-encoder reranking_. Код овог
приступа се прво постојећим механизмом претраге издваја мањи број
кандидата, након чега се они додатно рангирају помоћу _cross-encoder_
модела.

Разлика у односу на _bi-encoder_ приступ коришћен у семантичкој
претрази овог система јесте у начину обраде упита и текста. Код
_bi-encoder_ приступа се упит и текст кодирају независно у засебне
_embedding_ векторе, након чега се њихова сличност израчунава
поређењем добијених вектора. _Cross-encoder_, насупрот томе, прима упит
и текст кандидата заједно и директно процјењује колико је тај текст
релевантан за конкретан упит. Оваква директна интеракција може да
омогући прецизнију процјену релевантности, али захтијева засебну обраду
сваког пара упита и кандидата, због чега је рачунски захтјевнија.

Истраживања показују да _cross-encoder_ модели могу да постигну добре
резултате и у _zero-shot_ условима, односно када се примјењују на
податке из домена за који нису посебно обучавани или прилагођавани
@rosa2022crossencoders. _BEIR_ евалуација такође показује да модели
засновани на поновном рангирању у просјеку постижу добре резултате у
таквим условима, али уз веће рачунске трошкове @thakur2021beir.

Увођење _cross-encoder_ модела у овај систем би захтијевало додатни
корак обраде над кандидатима добијеним у првој фази претраге, као и
избор и евалуацију модела погодног за српски језик и корпус коришћен у
овом раду. Због тога је у тренутној имплементацији предност дата
једноставнијем приступу заснованом на _RRF_-у, док _cross-encoder
reranking_ представља могућност за будуће унапређење система.


=== Ограничења _RRF_-а

Иако је једноставан за примјену, _RRF_ није нужно оптималан начин
спајања резултата у сваком систему. Bruch и сарадници показују да
његови резултати могу да зависе од избора параметара, као и да
комбинација нормализованих лексичких и семантичких скорова може да
надмаши _RRF_ када су доступни подаци за подешавање параметара фузије
@bruch2023fusion.

У оквиру овог рада је _RRF_ ипак изабран јер не захтијева
нормализацију различитих скорова нити податке за обучавање параметара
фузије, а његова примјена над постојећим ранг-листама је једноставна и
рачунски јефтина. Оптимизација начина спајања резултата на основу
означеног скупа упита и релевантних епизода остаје могући правац даљег
развоја система.


== Архитектура: три ендпоинта

Систем излаже три одвојена ендпоинта за претрагу: *_`/search`_* за
лексичку претрагу описану у поглављу 6, *_`/search/semantic`_* за
семантичку претрагу описану у поглављу 7 и *_`/search/hybrid`_* за
хибридну претрагу описану у овом поглављу. Оваква организација
омогућава независно коришћење и тестирање сваког приступа, као и
њихово директно поређење приликом евалуације система у поглављу 9.

Хибридна претрага је имплементирана у сервису
*_`hybrid_search_service.py`_*, који користи постојеће функције
*_`search_podcasts()`_* и *_`semantic_search()`_*. На тај начин је
избјегнуто дуплирање логике лексичке и семантичке претраге, а њихови
резултати се у хибридном сервису само обједињују и поново рангирају
примјеном _RRF_ методе.

Сва три ендпоинта користе исти формат одговора (*_`SearchResult`_* и
*_`SearchResponse`_*), са пољима као што су *_`videoId`_*,
*_`channelName`_*, *_`episodeTitle`_*, *_`relevanceScore`_* и
*_`matches`_*. Захваљујући јединственом формату, _frontend_ апликација
и скрипта за евалуацију могу на исти начин да обрађују резултате сва
три приступа претрази.


== Ниво фузије резултата

Лексичка и семантичка претрага се не извршавају над истом јединицом
текста. Лексичка претрага ради над појединачним сегментима транскрипта,
док семантичка претрага ради над _chunk_-овима насталим груписањем више
узастопних сегмената. Разлози за избор ових грануларности су описани у
поглављима 6 и 7.

Због различите грануларности, резултате није практично директно спајати
на нивоу сегмента или _chunk_-а. Један _chunk_ може да обухвата више
сегмената, па резултати два система немају директно једнозначно
пресликавање.


=== Фузија на нивоу епизоде

Резултати лексичке и семантичке претраге се спајају на нивоу епизоде,
која представља њихов заједнички ниво. Сваки сегмент и сваки _chunk_
припадају тачно једној епизоди и садрже њен *_`video_id`_*, што
омогућава да се резултати прије примјене _RRF_ методе групишу и
рангирају према епизоди.

Овај приступ одговара и начину приказа резултата у корисничком
интерфејсу, гдје основни резултат представља епизода, док пронађени
сегменти и _chunk_-ови указују на релевантне дијелове њеног
транскрипта.


== Алгоритам фузије

Имплементација _RRF_ методе је подијељена у два корака. Најприје се
добијају рангиране листе епизода из лексичке и семантичке претраге,
након чега се оне спајају у јединствену листу примјеном _RRF_ формуле.
Алгоритам је приказан на слици @fig-algoritam-fuzije-rrf.

#figure(
  image("../slike/hibridizacija-algoritam.png"),
  caption: [Алгоритам фузије резултата примјеном _RRF_ методе]
) <fig-algoritam-fuzije-rrf>


=== Прикупљање рангираних листа

Функција *_`get_ranked_lists()`_* позива *_`search_podcasts()`_* и
*_`semantic_search()`_* са *_`page=1`_* и *_`page_size=MAX_TOTAL`_*,
при чему је *_`MAX_TOTAL = 100`_*. На тај начин се за фузију прикупља
до 100 најбоље рангираних епизода из сваког система претраге, као што
је приказано у листингу @lst:get-ranked-lists.

#figure(
  ```python
  def _get_ranked_lists(db: Session, query: str) -> tuple[list[str], list[str]]:
      cached = _lists_cache.get(query)
      if cached is not None:
          return cached["bm25_lista"], cached["vector_lista"]

      bm25_response = search_podcasts(query=query, page=1, page_size=MAX_TOTAL)
      vector_response = semantic_search(db, query=query, page=1, page_size=MAX_TOTAL)

      bm25_lista = [r["videoId"] for r in bm25_response["results"]]
      vector_lista = [r["videoId"] for r in vector_response["results"]]

      _lists_cache[query] = {
          "bm25_lista": bm25_lista,
          "vector_lista": vector_lista,
          "bm25_details": {r["videoId"]: r for r in bm25_response["results"]},
          "vector_details": {r["videoId"]: r for r in vector_response["results"]},
      }
      return bm25_lista, vector_lista
  ```,
  caption: [Прикупљање и кеширање рангираних листа _BM25_ и векторске претраге]
) <lst:get-ranked-lists>

Из добијених резултата се издвајају *_`videoId`_* вриједности уз
очување њиховог оригиналног поретка. Тако настају *_`bm25_lista`_* и
*_`vector_lista`_*, које представљају улаз у _RRF_ алгоритам. Остали
подаци о резултатима се чувају у *_`bm25_details`_* и
*_`vector_details`_* како би се касније могли искористити за формирање
коначног одговора. Механизам кеширања ових података је описан у
одјељку 8.8.


=== Спајање листа

Спајање двије ранг-листе је реализовано функцијом
*_`reciprocal_rank_fusion()`_*, приказаном у листингу
@lst:reciprocal-rank-fusion:

#figure(
  ```python
  def reciprocal_rank_fusion(
      bm25_lista: list[str], vector_lista: list[str], k: int = RRF_K
  ) -> list[tuple[str, float]]:
      scores: dict[str, float] = {}

      for rank, video_id in enumerate(bm25_lista, start=1):
          scores[video_id] = scores.get(video_id, 0.0) + 1.0 / (k + rank)

      for rank, video_id in enumerate(vector_lista, start=1):
          scores[video_id] = scores.get(video_id, 0.0) + 1.0 / (k + rank)

      return sorted(scores.items(), key=lambda x: x[1], reverse=True)
  ```,
  caption: [Имплементација _Reciprocal Rank Fusion_ (_RRF_) алгоритма за спајање рангираних листа]
) <lst:reciprocal-rank-fusion>

Функција пролази кроз обје листе и за сваку епизоду израчунава допринос
$1/(k + "rank")$. Ако се иста епизода појављује у обје листе, њени
доприноси се сабирају, док епизода присутна само у једној листи добија
допринос искључиво на основу своје позиције у тој листи. Након обраде
свих резултата, епизоде се сортирају опадајуће према добијеном _RRF_
скору, чиме се формира јединствена ранг-листа хибридне претраге.


== Пагинација хибридних резултата

Пагинација хибридне претраге се примјењује тек након спајања резултата.
Просљеђивање параметара *_`page`_* и *_`page_size`_* директно лексичкој
и семантичкој претрази није погодно, јер се коначни поредак епизода
одређује тек након примјене _RRF_ методе. Епизода која има нижу позицију
у једној листи може након фузије бити високо рангирана захваљујући својој
позицији у другој листи.

Због тога се за сваки нови упит најприје прикупља до
*_`MAX_TOTAL = 100`_* најбоље рангираних епизода из сваког система, као
што је приказано у листингу @lst:bm25-vector-search:

#figure(
  ```python
  bm25_response = search_podcasts(
      query=query,
      page=1,
      page_size=MAX_TOTAL
  )

  vector_response = semantic_search(
      db,
      query=query,
      page=1,
      page_size=MAX_TOTAL
  )
  ```,
  caption: [Покретање лексичке и векторске претраге за исти кориснички упит]
) <lst:bm25-vector-search>

Над тако добијеним листама се извршава _RRF_ фузија, чиме настаје
јединствена ранг-листа. Пагинација се затим примјењује над већ спојеним
резултатима, као што је приказано у листингу @lst:rrf-pagination.

#figure(
  ```python
  total = len(rrf_rezultat)
  offset = (page - 1) * page_size
  stranica = rrf_rezultat[offset : offset + page_size]
  ```,
  caption: [Пагинација резултата након _RRF_ фузије]
) <lst:rrf-pagination>

На овај начин се све странице истог упита добијају из истог кандидатског
скупа, па се поредак резултата и вриједност *_`total`_* не мијењају
приликом преласка са једне странице на другу. При томе *_`total`_*
представља број јединствених епизода добијених фузијом прикупљених
резултата, а не укупан број потенцијално релевантних епизода у читавом
корпусу.

Овај приступ захтијева да се већ при првом позиву прикупи до 100
резултата из сваког система, чак и ако корисник прегледа само прву
страницу. Тај компромис је прихваћен како би се обезбиједили стабилан
поредак и досљедна пагинација, док се поновљени позиви за исти упит
оптимизују механизмом кеширања описаним у наредном одјељку.


== Кеширање

Како би се избјегло поновно извршавање лексичке и семантичке претраге
приликом преласка између страница истог упита, прикупљене ранг-листе се
привремено чувају у меморији. Кеш је имплементиран као _Python_ рјечник.

Кључ представља текст корисничког упита, док се за њега чувају лексичка
и семантичка ранг-листа (*_`bm25_lista`_* и *_`vector_lista`_*), као и
подаци о резултатима потребни за касније формирање одговора. При
поновном позиву са истим упитом се ови подаци преузимају из меморије,
без поновног извршавања претраге у _Elasticsearch_-у и _PostgreSQL_-у.

У овој имплементацији је коришћен једноставан меморијски кеш умјесто
посебног система као што је _Redis_. Овакво рјешење је довољно за
локално покретање и евалуацију система, а истовремено не уводи додатну
инфраструктурну зависност.

Кеширају се изворне ранг-листе, а не коначна листа добијена _RRF_
методом. На тај начин се _RRF_ скор може поново израчунати над већ
доступним резултатима, без новог извршавања лексичке и семантичке
претраге.

Овако реализован кеш нема ограничење величине нити механизам истицања
записа, а везан је за меморију појединачног _backend_ процеса. Због тога
представља рјешење прилагођено тренутном начину употребе система. За
вишекорисничко или продукцијско окружење кеширање би се могло проширити
ограничењем величине, политиком избацивања старих записа или употребом
посебног система за кеширање.


== Филтрирање након фузије

Поред текстуалног упита, систем подржава филтрирање резултата према
називу епизоде, каналу и датуму објаве. У хибридној претрази се филтери
примјењују након _RRF_ фузије, односно над већ формираном ранг-листом
епизода.

Лексичка и семантичка претрага се зато најприје извршавају без ових
филтера, након чега се њихове ранг-листе спајају _RRF_ методом. Филтери
затим само уклањају епизоде које не задовољавају задате услове, без
промјене међусобног поретка преосталих резултата.

Овај приступ омогућава да основни _RRF_ ранг за исти текстуални упит
остане непромијењен без обзира на накнадно изабране филтере. Да су
филтери примјењивани прије претраге и фузије, свака комбинација филтера
би формирала другачији кандидатски скуп, па би се мијењале и позиције
епизода које улазе у _RRF_ формулу.

Провјера филтера је реализована функцијом *_`_passes_filters()`_*, као
што је приказано у листингу @lst:hybrid-filters.

#figure(
  ```python
  def _passes_filters(details, episode_name, channel_name, date_from, date_to) -> bool:
      if episode_name and episode_name.strip().lower() not in details["episodeTitle"].lower():
          return False
      if channel_name and details["channelName"] != channel_name:
          return False
      if date_from or date_to:
          published_str = details.get("publishedAt")
          if not published_str:
              return False
          published_date = datetime.strptime(published_str, "%Y-%m-%d").date()
          if date_from and published_date < datetime.strptime(date_from, "%Y-%m-%d").date():
              return False
          if date_to and published_date > datetime.strptime(date_to, "%Y-%m-%d").date():
              return False
      return True
  ```,
  caption: [Провјера филтера над резултатима хибридне претраге]
) <lst:hybrid-filters>

Филтер по називу епизоде се заснива на провјери да ли се задати текст
налази у називу, без употребе анализатора лексичке претраге. Филтер по
каналу захтијева тачно поклапање назива канала, док се датум објаве
провјерава у односу на задату доњу и горњу границу. Ако датумски филтер
постоји, а епизода нема познат датум објаве, она се искључује из
резултата.


=== Кеширање филтрираних резултата

Исти текстуални упит може да се користи са различитим комбинацијама
филтера. Због тога се филтрирани резултати чувају одвојено од
нефилтрираних ранг-листа, користећи кључ који обухвата упит и све
параметре филтрирања.

Промјена било ког филтера формира нови кључ. Филтрирање се тада поново
примјењује над већ кешираним резултатима претраге, без поновног
позивања лексичког и семантичког система.


== Претрага без текстуалног упита

Напредна претрага омогућава задавање само филтера, без текста упита. У
том случају не постоји текстуални сигнал на основу којег би лексичка
или семантичка претрага одређивале релевантност, па се _Elasticsearch_
и векторска претрага не извршавају. Умјесто тога, систем директно
претражује табелу *_`Video`_* у _PostgreSQL_ бази и примјењује задате
филтере према називу епизоде, каналу и датуму објаве. Ако нису задати
ни текстуални упит ни филтери, враћа се празна листа резултата.

Пошто у овом случају не постоји скор релевантности, пронађене епизоде
се сортирају према датуму објаве, од најновијих ка старијим. Формат
одговора остаје исти као код осталих начина претраге, чиме се задржава
јединствен начин обраде резултата на страни _frontend_ апликације.

При филтрирању према горњој граници датума се обухвата читав дан наведен
у параметру *_`date_to`_*. Пошто се у бази чува и вријеме објаве, као
искључива горња граница се користи почетак наредног дана. На тај начин
епизоде објављене током посљедњег дана задатог периода неће бити
изостављене због времена објаве.


== Приказ релевантних исјечака по епизоди

Хибридни резултат, поред ранга епизоде, садржи и временске исјечке
(*_`matches`_*) који указују на дијелове транскрипта препознате као
релевантне. Пошто лексичка и семантичка претрага ове исјечке проналазе
независно, у хибридном одговору се они обједињују и приказују у
јединственој листи.

Са лексичке стране се користе сегменти добијени кроз _Elasticsearch_
*_`inner_hits`_*. Како би хибридни резултат могао да прикаже већи број
релевантних дијелова исте епизоде, параметар *_`size`_* је повећан са 3
на 100. На тај начин може да се врати до 100 најбоље рангираних
сегмената за једну епизоду.

Са семантичке стране се користи функција *_`get_all_vector_matches()`_*,
која, за разлику од поступка рангирања епизода, не задржава само најбољи
_chunk_ по епизоди, већ групише све пронађене _chunk_-ове из
кандидатског скупа према *_`video_id`_*.


=== Усклађивање векторског кандидатског скупа

Кандидатски скуп који користи *_`get_all_vector_matches()`_* је усклађен
са скупом који се користи приликом семантичког рангирања. Његова
величина се зато не задаје независно, већ се изводи из исте константе,
као што је приказано у листингу @lst:vector-candidate-pool.

#figure(
  ```python
  from app.repositories.chunk_repository import CANDIDATE_MULTIPLIER

  VECTOR_CANDIDATE_POOL = MAX_TOTAL * CANDIDATE_MULTIPLIER
  ```,
  caption: [Одређивање величине скупа кандидата за векторску претрагу]
) <lst:vector-candidate-pool>

На овај начин се промјена *_`CANDIDATE_MULTIPLIER`_* примјењује на оба
поступка, чиме се избјегава неслагање између скупа _chunk_-ова
коришћених за рангирање и скупа из којег се формирају исјечци за приказ.


=== Спајање и сортирање исјечака

За сваку епизоду се лексички и семантички исјечци спајају у једну листу
и сортирају према временској позицији унутар видеа, као што је приказано
у листингу @lst:combine-hybrid-matches.

#figure(
  ```python
  bm25_matches = bm25_details.get(video_id, {}).get("matches", [])
  vector_matches = vector_matches_by_video.get(video_id, [])

  combined = bm25_matches + vector_matches
  combined.sort(key=lambda m: m["timestamp_seconds"])

  result["matches"] = combined
  ```,
  caption: [Спајање и хронолошко сортирање поклапања лексичке и векторске претраге]
) <lst:combine-hybrid-matches>

Корисник тако добија хронолошки уређен преглед релевантних дијелова
епизоде, без обзира на то да ли су пронађени лексичком или семантичком
претрагом.

Прикупљање додатних векторских исјечака захтијева још један упит над
табелом *_`Chunk`_*. Добијени подаци се кеширају заједно са осталим
резултатима упита, па се овај корак не понавља приликом преласка између
страница исте претраге.


== Ограничења и правци даљег развоја

Одређена ограничења семантичке претраге описана у поглављу 7 се преносе
и на хибридни приступ. Прије свега, тренутна имплементација не користи
минимални праг сличности за семантичке резултате, па и слабије повезани
кандидати могу да учествују у _RRF_ фузији. Одређивање одговарајућег
прага на основу евалуационог скупа представља један од могућих праваца
унапређења.

Вриједност параметра $k = 60$ је преузета из оригиналног рада о _RRF_
методи и није посебно оптимизована за корпус коришћен у овом раду
@cormack2009. Даља евалуација би могла да обухвати испитивање различитих
вриједности овог параметра, као и других начина фузије. Посебно,
комбинација нормализованих лексичких и семантичких скорова може да
надмаши _RRF_ када су доступни подаци за подешавање параметара фузије
@bruch2023fusion. Додатну могућност представља _cross-encoder
reranking_, описан у одјељку 8.2.2.

Простор за унапређење постоји и у погледу скалабилности. Меморијски кеш
нема ограничење величине нити механизам истицања записа, па би у
вишекорисничком или продукцијском окружењу било потребно увести
одговарајућу политику управљања кешом или посебан систем за кеширање.
Такође, код већег корпуса би требало испитати однос између величине
кандидатског скупа, времена одзива и квалитета хибридне претраге.