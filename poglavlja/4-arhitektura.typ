= Архитектура система

Систем описан у овом раду реализован је као скуп независних сервиса, међусобно повезаних преко заједничке мреже и покренутих помоћу _Docker Compose_-а, који представља алат за дефинисање и покретање апликација састављених од више контејнера једном командом @dockercompose. Контејнеризација омогућава да сваки сервис (база података, претраживачки систем, позадински сервис, кориснички интерфејс) буде изолован у сопственом окружењу, са тачно дефинисаним зависностима, при чему се цијели систем покреће и зауставља на исти начин, без обзира на то да ли се извршава на развојној машини или серверу. Систем чине пет сервиса на заједничкој мрежи (енг. _app-network_): база података (_PostgreSQL_ са _pgvector_ екстензијом), административни алат за базу (_pgAdmin4_), претраживачки систем (_Elasticsearch_), позадински сервис (_FastAPI_) и кориснички интерфејс (_Angular_).

== База података — _PostgreSQL_ и _pgvector_

_PostgreSQL_ у оквиру система представља централну базу података и извор истине (енг. _source of truth_), главни и поуздани извор за све структуриране податке. У њему се чувају метаподаци о каналима, епизодама и епизодним сегментима. Поред тога, захваљујући екстензији pgvector, у бази се чувају и векторске репрезентације текста (енг. _embeddings_), које се користе за семантичку претрагу описану у поглављу 7. _pgvector_ је екстензија отвореног кода која _PostgreSQL_-у додаје подршку за чување вектора и претрагу по њиховој сличности @pgvector. Разлози за избор _pgvector_-а у односу на друга рјешења, као што су _FAISS_, _Qdrant_, _Milvus_ и _Pinecone_, детаљно су образложени у поглављу 7.

За покретање базе користи се службена слика (енг. _image_) *_pgvector/pgvector:pg16_*, која садржи _PostgreSQL 16_ са уграђеном _pgvector_ екстензијом. Подаци се чувају у именованом простору за трајно чување података (енг. _volume_), чиме се обезбјеђује њихово очување и након поновног покретања контејнера. Спремност сервиса провјерава се помоћу _healthcheck_-а, који користи команду _pg_isready_. Конфигурација _PostgreSQL_ базе са _pgvector_ екстензијом се може погледати у листингу @lst:postgres-config.

#figure(
  ```yaml
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_DB=podcast_search
      - POSTGRES_USER=podcast_user
      - POSTGRES_PASSWORD=podcast_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U podcast_user"]
  ```,

  caption: [Конфигурација _PostgreSQL_ базе са _pgvector_ екстензијом]
) <lst:postgres-config>

За административни приступ бази података, који укључује преглед табела и ручно извршавање упита, коришћен је _pgAdmin4_. Административном интерфејсу приступа се путем веб интерфејса на порту _5050_.


== Претраживачки систем — _Elasticsearch_
_Elasticsearch_ сервис је заснован на прилагођеној _Docker_ слици, изграђеној на службеној слици *_elasticsearch:8.13.2_*. Слици је додат _analysis-icu_ додатак @elasticicu, који је коришћен за нормализацију и обраду ћириличног и латиничног текста, што је детаљније описано у поглављу 6. Прилагођена слика се дефинише сљедећим _Dockerfile_-ом, приказаним у листингу @lst:elasticsearch-dockerfile.

#figure(
  ```dockerfile
  FROM docker.elastic.co/elasticsearch/elasticsearch:8.13.2
  RUN bin/elasticsearch-plugin install --batch analysis-icu
  ```,

  caption: [_Dockerfile_ за изградњу прилагођене _Elasticsearch_ слике са _analysis-icu_ додатком]
) <lst:elasticsearch-dockerfile>

Сервис ради у режиму _single-node_, што значи да се _Elasticsearch_ извршава као један чвор, без повезивања са другим чворовима у кластеру. _X-Pack_ безбједносни механизми @elasticxpack, који обезбјеђују аутентификацију и контролу приступа _Elasticsearch_-у, су искључени, што одговара развојном и интерном окружењу у којем је систем коришћен. Спремност сервиса провјерава се помоћу _healthcheck_-а, који чека да статус кластера више не буде ред. Комплетна конфигурација индекса, анализатора и поступака индексирања и претраге описана је у поглављу 6.

== Позадински сервис — _FastAPI_

Позадински дио система реализован је кроз _Python 3.11_, уз коришћење оквира _FastAPI_ и _Uvicorn_ сервера, који имплементира _ASGI_ (енг. _Asynchronous Server Gateway Interface_) стандард и служи за покретање апликације и обраду _HTTP_ захтјева. _FastAPI_ је изабран због једноставне имплементације _API_-ја. Поред тога, _Python_ пружа развијен екосистем за машинско учење и обраду природног језика @fastapi, који је у овом систему коришћен и за генерисање векторских репрезентација помоћу библиотеке _sentence-transformers_.

За рад са базом података користи се _SQLAlchemy_ као _ORM_ (енг. _Object-Relational Mapping_), уз _psycopg2-binary_ као _PostgreSQL_ управљачки програм. Промјене структуре базе управљају се помоћу _Alembic_-а, док се за рад са векторским колонама користи _Python_ подршка за _pgvector_. Валидација конфигурације и података реализована је помоћу библиотека _Pydantic_ и _pydantic-settings_, док се вриједности из окружења учитавају помоћу библиотеке _python-dotenv_. Комуникација са _Elasticsearch_-ом остварује се преко службеног _Python_ клијента.

_Docker_ слика позадинског сервиса гради се у једној фази, уз коришћење кеширања слојева. Зависности наведене у датотеци _requirements.txt_ инсталирају се прије копирања изворног кода, што омогућава да се приликом измјене кода поново искористи постојећи слој са инсталираним зависностима, без њихове поновне инсталације. Модели преузети преко библиотека _Hugging Face_, укључујући модел за генерисање векторских репрезентација, чувају се у именованом _volume_-у (_hf_cache_). На тај начин се избјегава њихово поновно преузимање приликом поновног покретања контејнера.



== Кориснички интерфejs — _Angular_

Кориснички интерфејс реализован је у _Angular_-у @angular, а за његову изградњу користи се _Docker multi-stage build_ @dockermultistage, који раздваја фазу изградње апликације од фазе њеног покретања. У првој фази, заснованој на слици *_node:22-alpine_*, гради се продукцијска верзија апликације, док се у другој фази добијени статички фајлови копирају у минималну _nginx:alpine_ слику. На тај начин коначна слика садржи само фајлове неопходне за покретање апликације, без изворног кода и алата који су потребни само током изградње, чиме се смањује њена величина.

_Angular_ апликација реализована је као _SPA_ (енг. _Single Page Application_), што значи да се рутирање између страница обавља на страни клијента. Ово је посебно важно за директан приступ конкретној епизоди и одговарајућем тренутку у транскрипту. Због тога је _nginx_ подешен тако да захтјеве за путање које не представљају постојеће статичке фајлове прослиједи апликацији преко _index.html_, приказано у листингу @lst:nginx-spa.

#figure(
  ```nginx
  location / {
      try_files $uri $uri/ /index.html;
  }
  ```,

  caption: [Конфигурација _nginx_-а за просљеђивање захтјева _Angular SPA_ апликацији]
) <lst:nginx-spa>


Овакво подешавање омогућава да директан приступ одређеној рути или освјежавање странице функционише исправно, јер _nginx_ захтјев просљеђује _Angular_-овом механизму за рутирање умјесто да врати грешку _404_. Додатно, _nginx_ је подешен за компресију одговора помоћу _gzip_-а и кеширање статичких ресурса, као што су _JavaScript_ и _CSS_ фајлови, чиме се смањује количина података која се преноси приликом поновног учитавања апликације.

== Мрежа и покретање система

Сви сервиси комуницирају преко заједничке _Docker bridge_ мреже (_app-network_), што им омогућава међусобну комуникацију преко имена сервиса (нпр. позадински сервис приступа бази преко хоста _postgres_, а не _localhost_-а), без потребе за излагањем интерних портова ван контејнера. Покретање и заустављање, као и потпуно брисање базе приказане су у листингу @lst:docker-compose-commands кроз њихове команде.

#figure(
  ```bash
  # Покретање свих сервиса у позадини уз поновну изградњу слика
  docker-compose up -d --build

  # Заустављање и уклањање контејнера уз задржавање података у volume-има
  docker-compose down

  # Заустављање и уклањање контејнера и именованих volume-а
  docker-compose down -v
  ```,

  caption: [Команде за покретање и заустављање _Docker Compose_ сервиса]
) <lst:docker-compose-commands>