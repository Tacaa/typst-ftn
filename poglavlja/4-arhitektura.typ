= Архитектура система

Систем описан у овом раду је реализован као скуп независних сервиса, међусобно повезаних преко заједничке мреже и покренутих помоћу _Docker Compose_-а. _Docker Compose_ представља алат за дефинисање и покретање апликација састављених од више контејнера @dockercompose. Контејнеризација омогућава да сваки сервис буде изолован у сопственом окружењу, са тачно дефинисаним зависностима, док се цијели систем може покренути и зауставити на јединствен начин.

Систем чини пет сервиса повезаних заједничком мрежом (_app-network_): база података (_PostgreSQL_ са _pgvector_ екстензијом), административни алат за базу (_pgAdmin 4_), претраживачки систем (_Elasticsearch_), позадински сервис (_FastAPI_) и кориснички интерфејс (_Angular_).

== База података

_PostgreSQL_ представља централну базу података и главни извор структурираних података у систему (енг. _source of truth_). У њој се чувају метаподаци о каналима и епизодама, као и сегменти транскрипта. Поред тога, помоћу екстензије _pgvector_, у бази се чувају векторске репрезентације текста (енг. _embeddings_), које се користе за семантичку претрагу описану у поглављу 7. _pgvector_ је екстензија отвореног кода која _PostgreSQL_-у додаје подршку за чување вектора и претрагу према њиховој сличности @pgvector. Разлози за избор _pgvector_-а у односу на друга рјешења, као што су _FAISS_, _Qdrant_, _Milvus_ и _Pinecone_, детаљније су образложени у поглављу 7.

За покретање базе користи се слика *_pgvector/pgvector:pg16_*, заснована на _PostgreSQL_-у 16 и припремљена за коришћење _pgvector_ екстензије. Сама екстензија се у бази креира кроз _Alembic_ миграцију. Подаци се чувају у именованом _Docker_ волумену, чиме се обезбјеђује њихово чување и након поновног покретања контејнера. Спремност сервиса провјерава се помоћу _healthcheck_-а, који користи команду _pg_isready_. Конфигурација _PostgreSQL_ сервиса приказана је у листингу @lst:postgres-config.

#figure(
  ```yaml
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      - POSTGRES_DB=podcast_search
      - POSTGRES_USER=podcast_user
      - POSTGRES_PASSWORD=podcast_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U podcast_user"]
  ```,

  caption: [Конфигурација _PostgreSQL_ базе са _pgvector_ екстензијом]
) <lst:postgres-config>

За административни приступ бази података, укључујући преглед табела и ручно извршавање упита, коришћен је _pgAdmin 4_. Његовом веб интерфејсу приступа се преко порта _5050_.

== Претраживачки систем

_Elasticsearch_ сервис је заснован на прилагођеној _Docker_ слици изграђеној на службеној слици *_elasticsearch:8.13.2_*. У слику је додат _*analysis-icu*_ додатак @elasticicu, који омогућава напреднију _Unicode_ нормализацију и обраду текстуалних знакова. Његова примјена у прилагођеном анализатору детаљније је описана у поглављу 6. Прилагођена слика дефинисана је сљедећим _Dockerfile_-ом, приказаним у листингу @lst:elasticsearch-dockerfile.

#figure(
  ```dockerfile
  FROM docker.elastic.co/elasticsearch/elasticsearch:8.13.2
  RUN bin/elasticsearch-plugin install --batch analysis-icu
  ```,

  caption: [_Dockerfile_ за изградњу прилагођене _Elasticsearch_ слике са _*analysis-icu*_ додатком]
) <lst:elasticsearch-dockerfile>

Сервис ради у режиму _single-node_, што значи да се _Elasticsearch_ извршава као један чвор, без повезивања са другим чворовима у кластеру. _X-Pack_ безбједносни механизми @elasticxpack, који обезбјеђују аутентификацију и контролу приступа су искључени јер је систем коришћен у развојном и интерном окружењу. Спремност сервиса провјерава се помоћу _healthcheck_-а, који чека да статус кластера више не буде _red_. Комплетна конфигурација индекса и анализатора, као и поступци индексирања и претраге, описани су у поглављу 6.

== Позадински сервис

Позадински дио система реализован је у програмском језику _Python 3.11_, уз коришћење оквира _FastAPI_ и сервера _Uvicorn_. _Uvicorn_ имплементира _ASGI_ (енг. _Asynchronous Server Gateway Interface_) стандард и користи се за покретање апликације и обраду _HTTP_ захтјева. _FastAPI_ је коришћен за реализацију програмског интерфејса апликације (_API_) @fastapi. _Python_ је у систему коришћен и за генерисање векторских репрезентација текста помоћу библиотеке _sentence-transformers_.

За рад са базом података користи се _SQLAlchemy_ као _ORM_ (енг. _Object-Relational Mapping_), уз _psycopg2-binary_ као _PostgreSQL_ управљачки програм. Промјенама структуре базе управља се помоћу _Alembic_-а, док се за рад са векторским колонама користи _Python_ подршка за _pgvector_. Валидација конфигурације и података реализована је помоћу библиотека _Pydantic_ и _pydantic-settings_, док се вриједности из окружења учитавају помоћу библиотеке _python-dotenv_. Комуникација са _Elasticsearch_-ом остварује се преко службеног _Python_ клијента.

_Docker_ слика позадинског сервиса гради се у једној фази, уз коришћење кеширања слојева. Зависности наведене у датотеци _requirements.txt_ инсталирају се прије копирања изворног кода. На тај начин се приликом измјене кода може поново искористити постојећи слој са већ инсталираним зависностима, без њихове поновне инсталације. Модели преузети преко платформе _Hugging Face_, укључујући модел за генерисање векторских репрезентација, чувају се у именованом _Docker_ волумену (`hf_cache`). Тиме се избјегава њихово поновно преузимање након поновног покретања или изградње контејнера.

== Кориснички интерфејс

Кориснички интерфејс је реализован у _Angular_-у @angular, а за његову изградњу користи се _Docker multi-stage build_ @dockermultistage, који раздваја фазу изградње апликације од фазе њеног покретања. У првој фази, заснованој на слици *_node:22-alpine_*, гради се продукцијска верзија апликације, док се у другој фази добијене статичке датотеке копирају у минималну *_nginx:alpine_* слику. На тај начин коначна слика садржи само датотеке неопходне за покретање апликације, без изворног кода и алата потребних само током изградње, чиме се смањује њена величина.

_Angular_ апликација реализована је као _SPA_ (енг. _Single Page Application_), што значи да се рутирање између страница обавља на страни клијента. Ово је посебно важно за директан приступ конкретној епизоди и одговарајућем тренутку у транскрипту. Због тога је _nginx_ подешен тако да захтјеве за путање које не одговарају постојећим статичким датотекама просљеђује апликацији преко _index.html_, као што је приказано у листингу @lst:nginx-spa.

#figure(
  ```nginx
  location / {
      try_files $uri $uri/ /index.html;
  }
  ```,

  caption: [Конфигурација _nginx_-а за просљеђивање захтјева _Angular SPA_ апликацији]
) <lst:nginx-spa>

Овакво подешавање омогућава исправан директан приступ одређеној рути и освјежавање странице, јер _nginx_ захтјев просљеђује _Angular_-овом механизму за рутирање умјесто да врати грешку 404. Додатно, _nginx_ је подешен за компресију одговора помоћу _gzip_-а и кеширање статичких ресурса, као што су _JavaScript_ и _CSS_ датотеке, чиме се смањује количина података која се преноси приликом поновног учитавања апликације.

== Мрежа и покретање система

Сви сервиси комуницирају преко заједничке _Docker bridge_ мреже (_app-network_), што омогућава међусобну комуникацију преко имена сервиса. На примјер, позадински сервис приступа бази преко имена хоста _postgres_, а не _localhost_. На тај начин није потребно излагати све интерне портове сервиса хост систему.

Основне команде за покретање и заустављање система, као и за уклањање именованих волумена, приказане су у листингу @lst:docker-compose-commands.

#figure(
  ```bash
  # Покретање свих сервиса у позадини уз поновну изградњу слика
  docker compose up -d --build

  # Заустављање и уклањање контејнера уз задржавање података у волуменима
  docker compose down

  # Заустављање и уклањање контејнера и именованих волумена
  docker compose down -v
  ```,

  caption: [Команде за покретање и заустављање _Docker Compose_ сервиса]
) <lst:docker-compose-commands>