= Хибридизација резултата претраге (RRF)

У претходним поглављима описана су два приступа претрази истог корпуса: лексичка претрага заснована на _BM25_ алгоритму (Глава 6) и семантичка претрага заснована на _*embedding*_ векторима (Глава 7). Лексичка претрага даје предност поклапању термина из упита и документа, док семантичка претрага омогућава проналажење садржаја сличног значења и када не постоји потпуно поклапање коришћених ријечи. Због различитих начина одређивања релевантности, ова два приступа могу се међусобно допуњавати @luan2021sparse.

Luan и сарадници показују да _*sparse*_ модели имају предност при прецизном препознавању поклапања термина, док _*dense*_ модели боље препознају семантичку сличност између сродних појмова. Аутори такође показују да фиксне векторске репрезентације _*dual-encoder*_ модела имају ограничења при прецизном претраживању дужих докумената и да комбиновање _*sparse*_ и _*dense*_ репрезентација може побољшати резултате претраге @luan2021sparse. Комбиновање лексичке и семантичке претраге у јединствену ранг-листу представља хибридну претрагу @elastichybrid.

У овом систему резултати лексичке и семантичке претраге обједињују се методом _*Reciprocal Rank Fusion*_ (*_RRF_*). У наставку поглавља описани су принцип рада и разлози избора _RRF_ методе, а затим и начин њене примјене у имплементираном систему.

== _Reciprocal Rank Fusion_

_*Reciprocal Rank Fusion*_ (_*RRF*_) је метода за спајање више рангираних листи у јединствену ранг-листу @cormack2009. За разлику од приступа који директно комбинују скорове различитих система, _*RRF*_ се заснива искључиво на позицији резултата у свакој листи. Због тога није потребно претходно усклађивати, односно нормализовати, _*BM25*_ скорове и вриједности косинусне сличности @cormack2009.


За документ \(d\) и скуп рангираних листи \(R\), RRF скор дефинише се као @cormack2009:

$ "RRF"(d) = sum_(r in R) 1 / (k + "rank"_r(d)) $

гдје _`"rank"_r(d)`_ представља позицију документа \(d\) у листи \(r\), док је \(k\) константа која ублажава утицај веома високих позиција. У имплементацији овог система, ако се епизода не појављује у једној од листи резултата, та листа једноставно не доприноси њеном _RRF_ скору.

=== Улога константе \(k\)

Константа \(k\) смањује разлику у доприносу између резултата који се налазе на самом врху листе. Без ње би прва позиција доприносила \(1\), а друга \(1/2\), док са \(k = 60\) њихови доприноси износе \(1/61\) и \(1/62\). На тај начин једна веома висока позиција има мањи утицај на коначни резултат.

У оригиналном раду Cormack и сарадници користили су \(k = 60\). Ова вриједност изабрана је током пилот-експеримента и задржана у каснијој евалуацији; аутори наводе да је била близу оптималне, али и да избор конкретне вриједности није био пресудан за резултате @cormack2009. Иста вриједност коришћена је и у овом систему.

=== Интуиција кроз примјер

Претпоставимо да лексичка претрага за исти упит рангира епизоде \(A\), \(B\) и \(C\) на позиције 1, 2 и 3, док их семантичка претрага поставља на позиције 2, 4 и 1. За \(k = 60\) добија се:

$ "RRF"(A) = 1/(60+1) + 1/(60+2) = 0.03252 $

$ "RRF"(B) = 1/(60+2) + 1/(60+4) = 0.03175 $

$ "RRF"(C) = 1/(60+3) + 1/(60+1) = 0.03227 $

Коначан поредак је, према томе, \(A > C > B\). Епизоде \(A\) и \(C\) имају највећи скор јер су високо позициониране у обје листе. _RRF_ на тај начин омогућава да резултат који је добро рангиран у више система добије допринос од сваког од њих, без директног поређења њихових оригиналних скорова.

== Избор RRF-а у односу на алтернативне приступе

При избору начина спајања резултата разматрана су два алтернативна приступа: комбиновање оригиналних скорова лексичке и семантичке претраге и додатно рангирање резултата помоћу _*cross-encoder*_ модела.

=== Комбиновање скорова

Један од начина спајања резултата јесте комбиновање _*BM25*_ скора и косинусне сличности, на примјер њиховом пондерисаном сумом. Међутим, ове вриједности нису директно упоредиве јер потичу из различитих модела рангирања и могу имати различите расподјеле и опсеге. Због тога је при оваквом приступу потребно претходно нормализовати скорове, на примјер _*min-max*_ нормализацијом, а затим одредити тежину сваког сигнала @opensearchhybrid.

_*RRF*_ избјегава потребу за усклађивањем скорова јер користи позицију резултата у појединачним ранг-листама, а не њихове оригиналне вриједности. Због једноставности и одсуства потребе за подешавањем тежина, овај приступ је погодан за систем развијен у оквиру овог рада.

=== _Cross-encoder reranking_

Друга разматрана могућност био је _*cross-encoder reranking*_. Код овог приступа прво се постојећим механизмом претраге издваја мањи број кандидата, након чега се они додатно рангирају помоћу _*cross-encoder*_ модела.

Разлика у односу на _*bi-encoder*_ приступ коришћен у семантичкој претрази овог система јесте у начину обраде упита и текста. Код _*bi-encoder*_ приступа упит и текст кодирају се независно у засебне _*embedding*_ векторе, након чега се њихова сличност израчунава поређењем добијених вектора. _*Cross-encoder*_, насупрот томе, прима упит и текст кандидата заједно и директно процјењује колико је тај текст релевантан за конкретан упит. Оваква директна интеракција омогућава прецизнију процјену релевантности, али захтијева засебну обраду сваког пара упита и кандидата, због чега је рачунски захтјевнија.

Истраживања показују да _*cross-encoder*_ модели могу постићи добре резултате и у _*zero-shot*_ условима, односно када се примјењују на податке из домена за који нису посебно обучавани или прилагођавани @rosa2022crossencoders. *BEIR* евалуација такође показује да модели засновани на поновном рангирању у просјеку постижу добре резултате у таквим условима, али уз веће рачунске трошкове @thakur2021beir.

Увођење _*cross-encoder*_ модела у овај систем захтијевало би додатни корак обраде над кандидатима добијеним у првој фази претраге, као и избор и евалуацију модела погодног за српски језик и корпус коришћен у овом раду. Због тога је у тренутној имплементацији предност дата једноставнијем приступу заснованом на _*RRF*_-у, док _*cross-encoder reranking*_ представља могућност за будуће унапређење система.


=== Ограничења RRF-а

Иако је једноставан за примјену, _*RRF*_ није нужно оптималан начин спајања резултата у сваком систему. Bruch и сарадници показују да његови резултати могу зависити од избора параметара, као и да комбинација нормализованих лексичких и семантичких скорова може надмашити _*RRF*_ када су доступни подаци за подешавање параметара фузије @bruch2023fusion.

У оквиру овог рада _*RRF*_ је ипак изабран јер не захтијева нормализацију различитих скорова нити податке за обучавање параметара фузије, а његова примјена над постојећим ранг-листама је једноставна и рачунски јефтина. Оптимизација начина спајања резултата на основу означеног скупа упита и релевантних епизода остаје могући правац даљег развоја система.


== Архитектура — три ендпоинта

Систем излаже три одвојена ендпоинта за претрагу: _*`/search`*_ за лексичку претрагу описану у поглављу 6, _*`/search/semantic`*_ за семантичку претрагу описану у поглављу 7 и _*`/search/hybrid`*_ за хибридну претрагу описану у овом поглављу. Оваква организација омогућава независно коришћење и тестирање сваког приступа, као и њихово директно поређење приликом евалуације система у поглављу 9.

Хибридна претрага имплементирана је у сервису *_`hybrid_search_service.py`_*, који користи постојеће функције *_`search_podcasts()`_* и _*`semantic_search()`*_. На тај начин избјегнуто је дуплирање логике лексичке и семантичке претраге, а њихови резултати се у хибридном сервису само обједињују и поново рангирају примјеном _*RRF*_ методе.

Сва три ендпоинта користе исти формат одговора (_*SearchResult*_ и _*SearchResponse*_), са пољима као што су _videoId_, _channelName_, _episodeTitle_, _relevanceScore_ и _matches_. Захваљујући јединственом формату, _*frontend*_ апликација и скрипта за евалуацију могу на исти начин обрађивати резултате сва три приступа претрази.

== Ниво фузије резултата

Лексичка и семантичка претрага не извршавају се над истом јединицом текста. Лексичка претрага ради над појединачним сегментима транскрипта, док семантичка претрага ради над _chunk_-овима насталим груписањем више узастопних сегмената. Разлози за избор ових грануларности описани су у главама 6 и 7.

Због различите грануларности, резултате није практично директно спајати на нивоу сегмента или _chunk_-а. Један _chunk_ може обухватати више сегмената, па резултати два система немају директно једнозначно пресликавање.


=== Фузија на нивоу епизоде

Резултати лексичке и семантичке претраге спајају се на нивоу епизоде, која представља њихов заједнички ниво. Сваки сегмент и сваки _chunk_ припадају тачно једној епизоди и садрже њен _video_id_, што омогућава да се резултати прије примјене _RRF_ методе групишу и рангирају према епизоди.

Овај приступ одговара и начину приказа резултата у корисничком интерфејсу, гдје основни резултат представља епизода, док пронађени сегменти и _chunk_-ови указују на релевантне дијелове њеног транскрипта.

== Алгоритам фузије

Имплементација _RRF_ методе подијељена је у два корака. Најприје се добијају рангиране листе епизода из лексичке и семантичке претраге, након чега се оне спајају у јединствену листу примјеном _RRF_ формуле. Алгоритам је приказан на слици @fig-algoritam-fuzije-rrf.

#figure(
  image("../slike/hibridizacija-algoritam.png"),
  caption: [Алгоритам фузије резултата примјеном _RRF_ методе]
) <fig-algoritam-fuzije-rrf>

=== Прикупљање рангираних листи

Функција _*get_ranked_lists()*_ позива _*search_podcasts()*_ и _*semantic_search()*_ са _page=1_ и _page_size=MAX_TOTAL_, при чему је _MAX_TOTAL = 100_. На тај начин за фузију се прикупља до 100 најбоље рангираних епизода из сваког система претраге, приказано у листингу @lst:get-ranked-lists.

#figure(
  ```python
  def _get_ranked_lists(db: Session, query: str) -> tuple[list[str], list[str]]:
      cached = _lists_cache.get(query)
      if cached is not None:
          return cached["bm25_lista"], cached["vector_lista"]

      bm25_response = search_podcasts(query=query, page=1, page_size=MAX_TOTAL)
      vector_response = semantic_search(db, query=query, page=1, page_size=MAX_TOTAL)

      bm25_lista = [r["videoId"] for r in bm25_response["results"]]
      vector_lista = [r["videoId"] for r in vector_response["results"]]

      _lists_cache[query] = {
          "bm25_lista": bm25_lista,
          "vector_lista": vector_lista,
          "bm25_details": {r["videoId"]: r for r in bm25_response["results"]},
          "vector_details": {r["videoId"]: r for r in vector_response["results"]},
      }
      return bm25_lista, vector_lista
  ```,

  caption: [Прикупљање и кеширање рангираних листи _BM25_ и векторске претраге]
) <lst:get-ranked-lists>

Из добијених резултата издвајају се _videoId_ вриједности уз очување њиховог оригиналног поретка. Тако настају _bm25_lista_ и _vector_lista_, које представљају улаз у _RRF_ алгоритам. Остали подаци о резултатима чувају се у _bm25_details_ и _vector_details_ како би се касније могли искористити за формирање коначног одговора. Механизам кеширања ових података описан је у одјељку 8.8.

=== Спајање листи

Спајање двије ранг-листе реализовано је функцијом _reciprocal_rank_fusion()_, приказаном у листингу @lst:reciprocal-rank-fusion:

#figure(
  ```python
  def reciprocal_rank_fusion(
      bm25_lista: list[str], vector_lista: list[str], k: int = RRF_K
  ) -> list[tuple[str, float]]:
      scores: dict[str, float] = {}

      for rank, video_id in enumerate(bm25_lista, start=1):
          scores[video_id] = scores.get(video_id, 0.0) + 1.0 / (k + rank)

      for rank, video_id in enumerate(vector_lista, start=1):
          scores[video_id] = scores.get(video_id, 0.0) + 1.0 / (k + rank)

      return sorted(scores.items(), key=lambda x: x[1], reverse=True)
  ```,

  caption: [Имплементација Reciprocal Rank Fusion (RRF) алгоритма за спајање рангираних листи]
) <lst:reciprocal-rank-fusion>

Функција пролази кроз обје листе и за сваку епизоду израчунава допринос \(1/(k+rank)\). Ако се иста епизода појављује у обје листе, њени доприноси се сабирају, док епизода присутна само у једној листи добија допринос искључиво на основу своје позиције у тој листи. Након обраде свих резултата, епизоде се сортирају опадајуће према добијеном _RRF_ скору, чиме се формира јединствена ранг-листа хибридне претраге.


== Пагинација хибридних резултата

Пагинација хибридне претраге примјењује се тек након спајања резултата. Просљеђивање параметара _page_ и _page_size_ директно лексичкој и семантичкој претрази није погодно, јер се коначни поредак епизода одређује тек након примјене _RRF_ методе. Епизода која има нижу позицију у једној листи може након фузије бити високо рангирана захваљујући својој позицији у другој листи.

Због тога се за сваки нови упит најприје прикупља до *_`MAX_TOTAL = 100`_* најбоље рангираних епизода из сваког система, приказаних у листингу @lst:bm25-vector-search:

#figure(
  ```python
  bm25_response = search_podcasts(
      query=query,
      page=1,
      page_size=MAX_TOTAL
  )

  vector_response = semantic_search(
      db,
      query=query,
      page=1,
      page_size=MAX_TOTAL
  )
  ```,

  caption: [Покретање лексичке и векторске претраге за исти кориснички упит]
) <lst:bm25-vector-search>

Над тако добијеним листама извршава се _RRF_ фузија, чиме настаје јединствена ранг-листа. Пагинација се затим примјењује над већ спојеним резултатима приказано у листингу @lst:rrf-pagination.

#figure(
  ```python
  total = len(rrf_rezultat)
  offset = (page - 1) * page_size
  stranica = rrf_rezultat[offset : offset + page_size]
  ```,

  caption: [Пагинација резултата након RRF фузије]
) <lst:rrf-pagination>

На овај начин све странице истог упита добијају се из истог кандидатског скупа, па се поредак резултата и вриједност _total_ не мијењају приликом преласка са једне странице на другу. При томе _total_ представља број јединствених епизода добијених фузијом прикупљених резултата, а не укупан број потенцијално релевантних епизода у читавом корпусу.

Овај приступ захтијева да се већ при првом позиву прикупи до 100 резултата из сваког система, чак и ако корисник прегледа само прву страницу. Тај компромис је прихваћен како би се обезбиједили стабилан поредак и досљедна пагинација, док се поновљени позиви за исти упит оптимизују механизмом кеширања описаним у наредном одјељку.


== Кеширање

Како би се избјегло поновно извршавање лексичке и семантичке претраге приликом преласка између страница истог упита, прикупљене ранг-листе привремено се чувају у меморији. Кеш је имплементиран као _Python_ рјечник.

Кључ представља текст корисничког упита, док се за њега чувају лексичка и семантичка ранг-листа (_bm25_lista_ и _vector_lista_), као и подаци о резултатима потребни за касније формирање одговора. При поновном позиву са истим упитом ови подаци преузимају се из меморије, без поновног извршавања претраге у _Elasticsearch_-у и _PostgreSQL_-у.

У овој имплементацији коришћен је једноставан меморијски кеш умјесто посебног система као што је *_Redis_*. Овакво рјешење довољно је за локално покретање и евалуацију система, а истовремено не уводи додатну инфраструктурну зависност.

Кеширају се изворне ранг-листе, а не коначна листа добијена _RRF_ методом. На тај начин _RRF_ скор се може поново израчунати над већ доступним резултатима, без новог извршавања лексичке и семантичке претраге.

Овако реализован кеш нема ограничење величине нити механизам истицања записа, а везан је за меморију појединачног _backend_ процеса. Због тога представља рјешење прилагођено тренутном начину употребе система. За вишекорисничко или продукцијско окружење кеширање би се могло проширити ограничењем величине, политиком избацивања старих записа или употребом посебног система за кеширање.


== Филтрирање након фузије

Поред текстуалног упита, систем подржава филтрирање резултата према називу епизоде, каналу и датуму објаве. У хибридној претрази филтери се примјењују након _RRF_ фузије, односно над већ формираном ранг-листом епизода.

Лексичка и семантичка претрага зато се најприје извршавају без ових филтера, након чега се њихове ранг-листе спајају _RRF_ методом. Филтери затим само уклањају епизоде које не задовољавају задате услове, без промјене међусобног поретка преосталих резултата.

Овај приступ омогућава да основни _RRF_ ранг за исти текстуални упит остане непромијењен без обзира на накнадно изабране филтере. Да су филтери примјењивани прије претраге и фузије, свака комбинација филтера формирала би другачији кандидатски скуп, па би се мијењале и позиције епизода које улазе у _RRF_ формулу.

Провјера филтера реализована је функцијом `_passes_filters()`, приказано у листингу @lst:hybrid-filters

#figure(
  ```python
  def _passes_filters(details, episode_name, channel_name, date_from, date_to) -> bool:
      if episode_name and episode_name.strip().lower() not in details["episodeTitle"].lower():
          return False
      if channel_name and details["channelName"] != channel_name:
          return False
      if date_from or date_to:
          published_str = details.get("publishedAt")
          if not published_str:
              return False
          published_date = datetime.strptime(published_str, "%Y-%m-%d").date()
          if date_from and published_date < datetime.strptime(date_from, "%Y-%m-%d").date():
              return False
          if date_to and published_date > datetime.strptime(date_to, "%Y-%m-%d").date():
              return False
      return True
  ```,

  caption: [Провјера филтера над резултатима хибридне претраге]
) <lst:hybrid-filters>

Филтер по називу епизоде заснива се на провјери да ли се задати текст налази у називу, без употребе анализатора лексичке претраге. Филтер по каналу захтијева тачно поклапање назива канала, док се датум објаве провјерава у односу на задату доњу и горњу границу. Ако датумски филтер постоји, а епизода нема познат датум објаве, она се искључује из резултата.

=== Кеширање филтрираних резултата

Исти текстуални упит може се користити са различитим комбинацијама филтера. Због тога се филтрирани резултати чувају одвојено од нефилтрираних ранг-листи, користећи кључ који обухвата упит и све параметре филтрирања.

Промјена било ког филтера формира нови кључ. Филтрирање се тада поново примјењује над већ кешираним резултатима претраге, без поновног позивања лексичког и семантичког система.


== Претрага без текстуалног упита

Напредна претрага омогућава задавање само филтера, без текста упита. У том случају не постоји текстуални сигнал на основу којег би лексичка или семантичка претрага одређивале релевантност, па се _Elasticsearch_ и векторска претрага не извршавају. Умјесто тога, систем директно претражује табелу _Video_ у _PostgreSQL_ бази и примјењује задате филтере према називу епизоде, каналу и датуму објаве. Ако нису задати ни текстуални упит ни филтери, враћа се празна листа резултата.

Пошто у овом случају не постоји скор релевантности, пронађене епизоде сортирају се према датуму објаве, од најновијих ка старијим. Формат одговора остаје исти као код осталих начина претраге, чиме се задржава јединствен начин обраде резултата на страни _frontend_ апликације.

При филтрирању према горњој граници датума обухвата се читав дан наведен у параметру _date_to_. Пошто се у бази чува и вријеме објаве, као искључива горња граница користи се почетак наредног дана. На тај начин епизоде објављене током посљедњег дана задатог периода неће бити изостављене због времена објаве.


== Приказ релевантних исјечака по епизоди

Хибридни резултат, поред ранга епизоде, садржи и временске исјечке (_matches_) који указују на дијелове транскрипта препознате као релевантне. Пошто лексичка и семантичка претрага ове исјечке проналазе независно, у хибридном одговору они се обједињују и приказују у јединственој листи.

Са лексичке стране користе се сегменти добијени кроз _Elasticsearch_ _inner_hits_. Како би хибридни резултат могао приказати већи број релевантних дијелова исте епизоде, параметар _size_ повећан је са 3 на 100. На тај начин може се вратити до 100 најбоље рангираних сегмената за једну епизоду.

Са семантичке стране користи се функција _get_all_vector_matches()_, која, за разлику од поступка рангирања епизода, не задржава само најбољи _chunk_ по епизоди, већ групише све пронађене _chunk_-ове из кандидатског скупа према _video_id_.

=== Усклађивање векторског кандидатског скупа

Кандидатски скуп који користи _get_all_vector_matches()_ усклађен је са скупом који се користи приликом семантичког рангирања. Његова величина зато се не задаје независно, већ се изводи из исте константе @lst:vector-candidate-pool.

#figure(
  ```python
  from app.repositories.chunk_repository import CANDIDATE_MULTIPLIER

  VECTOR_CANDIDATE_POOL = MAX_TOTAL * CANDIDATE_MULTIPLIER
  ```,

  caption: [Одређивање величине скупа кандидата за векторску претрагу]
) <lst:vector-candidate-pool>

На овај начин промјена _CANDIDATE_MULTIPLIER_ примјењује се на оба поступка, чиме се избјегава неслагање између скупа _chunk_-ова коришћених за рангирање и скупа из којег се формирају исјечци за приказ.

=== Спајање и сортирање исјечака

За сваку епизоду лексички и семантички исјечци спајају се у једну листу и сортирају према временској позицији унутар видеа, приказано у листингу @lst:combine-hybrid-matches.

#figure(
  ```python
  bm25_matches = bm25_details.get(video_id, {}).get("matches", [])
  vector_matches = vector_matches_by_video.get(video_id, [])

  combined = bm25_matches + vector_matches
  combined.sort(key=lambda m: m["timestamp_seconds"])

  result["matches"] = combined
  ```,

  caption: [Спајање и хронолошко сортирање поклапања лексичке и векторске претраге]
) <lst:combine-hybrid-matches>

Корисник тако добија хронолошки уређен преглед релевантних дијелова епизоде, без обзира на то да ли су пронађени лексичком или семантичком претрагом.

Прикупљање додатних векторских исјечака захтијева још један упит над табелом _Chunk_. Добијени подаци кеширају се заједно са осталим резултатима упита, па се овај корак не понавља приликом преласка између страница исте претраге.



== Ограничења и правци даљег развоја

Одређена ограничења семантичке претраге описана у поглављу 7 преносе се и на хибридни приступ. Прије свега, тренутна имплементација не користи минимални праг сличности за семантичке резултате, па и слабије повезани кандидати могу учествовати у _RRF_ фузији. Одређивање одговарајућег прага на основу евалуационог скупа представља један од могућих праваца унапређења.

Вриједност параметра \(k = 60\) преузета је из оригиналног рада о _RRF_ методи и није посебно оптимизована за корпус коришћен у овом раду @cormack2009. Даља евалуација могла би обухватити испитивање различитих вриједности овог параметра, као и других начина фузије. Посебно, комбинација нормализованих лексичких и семантичких скорова може надмашити _RRF_ када су доступни подаци за подешавање параметара фузије @bruch2023fusion. Додатну могућност представља _cross-encoder reranking_, описан у одјељку 8.2.2.

Простор за унапређење постоји и у погледу скалабилности. Меморијски кеш нема ограничење величине нити механизам истицања записа, па би у вишекорисничком или продукцијском окружењу било потребно увести одговарајућу политику управљања кешом или посебан систем за кеширање. Такође, код већег корпуса требало би испитати однос између величине кандидатског скупа, времена одзива и квалитета хибридне претраге.

